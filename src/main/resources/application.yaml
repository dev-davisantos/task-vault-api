spring:
  application:
    name: TaskVaultApi

  datasource:
    url: jdbc:postgresql://localhost:5432/task_vault_db
    username: postgres
    password: postgres

  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update